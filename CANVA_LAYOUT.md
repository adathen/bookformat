# Canva 版面編排紀錄

用 Canva 對「會發光的迴力鏢」小書內容 PDF（尚未拼版、依頁碼順序排列的原始
內容）做每頁的版面編排優化，跟本 repo 的拼版程式（`docs/js/pdfImposer.js`）
是分開的工作——這裡只處理「每一頁內容排版好不好看」，不動拼版邏輯。

## 設計檔案

- 標題：`會發光的迴力鏢小書0711.pdf`
- Canva 設計 ID：`DAHRy9uMyq8`
- 編輯連結：<https://www.canva.com/design/DAHRy9uMyq8/CQeCYG4s9OaQb2hl7hWUFA/edit>
- 帳號：adathen@gmail.com
- 共 20 頁，頁面尺寸 794×1123（A4 @96dpi），跟小書轉換器桌面版/網頁版讀出的
  頁面幾何一致

## 進度

- [x] 第 2 頁 — 已重新編排並存檔
- [ ] 第 1 頁（封面，結構特殊，另外處理）
- [ ] 第 3–20 頁（其中「給家長的話」等幾頁是表格內容，跟一般「一圖一文」頁
      結構不同，需要另外設計）

## 編排原則（保持原有圖文內容，只優化版面）

- 內容欄寬統一 684px，左右邊界各 55px（頁寬 794px）
- 圖片放大置中在頁面上方，寬度約 580px（維持長寬比）
- 內文段落：字級沿用原設定（約 18.7px），左對齊，684px 寬度讓 Canva 自動
  換行（比原本每個文字方塊各自不同寬度、換行位置不一致要統一）
- 強調句（例如「我不開心！」這類單獨成句的對白）：置中對齊、字級加大，當
  作 pull-quote 處理
- 頁碼維持在頁面下方置中，字級不變
- 區塊間距：圖片與內文間約 35px，內文段落之間約 25px

## 重要技術筆記

- **`read-design` 回傳的 `pos:` 是 `top,left` 順序，不是 `left,top`**。
  第一次讀取時誤判成 `left,top`，導致以為原始匯入座標很混亂（例如文字
  x 座標超出 794 頁寬）；實際上用正確順序重新解讀後，原始座標大致合理。
  這不影響最終編排結果（用 `edit-design` 的 `position_element` 時是用具名
  參數 `top`/`left`，不是靠解析 `pos:` 字串），但往後讀 `read-design` 輸出
  時要記得這個順序。
- PDF 匯入 Canva 後，原本一行文字常被拆成好幾個獨立文字方塊（例如「這時，
  一個溫柔的身影出現，」「是大家最信任的——」「袋鼠叔 叔。」三個方塊疊在
  一起才拼成一行）。重新編排時要先判斷哪些方塊其實是同一句話的碎片，用
  `replace_text` 合併成一個完整文字方塊、刪除多餘的碎片方塊，再統一設定
  寬度讓文字自然換行——不要嘗試逐一微調碎片位置去湊出原本的樣子。
- `resize_element` 對 TEXT 元素只需給 `width`，高度會自動依換行結果算出來；
  拿到新高度後才能算出下一個區塊該放在哪個 `top`，所以是「先 resize 拿到
  各區塊實際高度 → 再依高度往下疊 position_element」兩階段流程。
- 剛 `commit` 完的頁面，`read-design` 抓到的縮圖有時還是舊版（Canva 那邊的
  縮圖 CDN 快取），但用 `design_content`（純文字讀取）可以立即確認內容已經
  正確更新——縮圖晚一點才會跟上。
